// Flip to true only when exporting the locked-down build for the dev hand-off zip.
// Keep false for normal design work — it re-enables the Dev Panel, Classic mode,
// the brand-off look, and the in-progress Campaign-section actions.
export const HANDOFF_MODE = false;
